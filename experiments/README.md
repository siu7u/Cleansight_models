# 个人实验总索引

本目录只整理当前分支上个人新增/修改的实验内容，不重排远端仓库已有的 `framework/`、`benchmark/`、
`registry/`、`legacy/` 等基础结构。所有路径均以 `Cleansight_models/` 为仓库根目录。

当前主线：

- [YOLO 训练、后处理、tracking 与性能评测](yolo/README.md)

本地结果位于 `runs/`，默认被 `.gitignore` 排除；README 中列出的配置、报告、JSON、视频和权重若需远端复现，
必须通过模型资产/对象存储或明确筛选后另行交付。
